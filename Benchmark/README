This .zip contains the datasets used for the benchmark approach of our paper.
Specifically, each row corresponds to each instance and the columns are the attributes of that instance. The final column is the label of the instance for the classification problem.
